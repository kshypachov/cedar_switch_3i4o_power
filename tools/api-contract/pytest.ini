# Pins the rootdir so `pytest` finds the same tests wherever it is invoked from,
# which matters because CI runs it from the repository root and a developer runs
# it from here.
[pytest]
testpaths = tests
addopts = -q --strict-markers
