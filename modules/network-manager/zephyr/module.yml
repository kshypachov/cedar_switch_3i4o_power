# Zephyr module declaration for the network manager.
#
# Registered by the application via ZEPHYR_EXTRA_MODULES (see the app
# CMakeLists.txt), the same way as the other modules here.
build:
  cmake: .
  kconfig: lib/Kconfig
