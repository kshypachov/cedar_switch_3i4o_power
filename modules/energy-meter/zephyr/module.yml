# Zephyr module declaration for energy-meter.
#
# Registered by the application via ZEPHYR_EXTRA_MODULES (see the app
# CMakeLists.txt). dts_root brings the cedar,eeprom-flash binding.
build:
  cmake: .
  kconfig: Kconfig
  settings:
    dts_root: .
