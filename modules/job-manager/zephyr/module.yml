# Zephyr module declaration for the job manager.
#
# Registered by the application via ZEPHYR_EXTRA_MODULES (see the app
# CMakeLists.txt), the same way as settings-registry.
build:
  cmake: .
  kconfig: lib/Kconfig
