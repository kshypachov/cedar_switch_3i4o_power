CONFIG_ZTEST=y
CONFIG_JOB_MANAGER=y

# Small pools so eviction and exhaustion are reachable in a test rather than
# theoretical. The production defaults are 16 / 256.
CONFIG_JOB_MANAGER_MAX_JOBS=4
CONFIG_JOB_MANAGER_DEDUPE_SLOTS=16
CONFIG_JOB_MANAGER_RETENTION_SECONDS=900
