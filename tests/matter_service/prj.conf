CONFIG_ZTEST=y
CONFIG_MATTER_SERVICE=y

# Small pools, so every limit is reachable in a test rather than argued about.
# The production defaults are in lib/Kconfig.
CONFIG_MATTER_SERVICE_MAX_FABRICS=3
CONFIG_MATTER_SERVICE_REQUEST_SLOTS=2
