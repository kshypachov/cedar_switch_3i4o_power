CONFIG_ZTEST=y
CONFIG_COPROCESSOR_MANAGER=y

# Production values. The clock is the fake's, so the real numbers cost nothing
# and the tests assert on the timings the board runs with.
CONFIG_COPROCESSOR_MANAGER_RX_STOP_TIMEOUT_MS=100
CONFIG_COPROCESSOR_MANAGER_RX_QUIET_STEP_MS=5
CONFIG_COPROCESSOR_MANAGER_RESET_WINDOW_MS=3000
