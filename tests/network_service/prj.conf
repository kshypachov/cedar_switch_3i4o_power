CONFIG_ZTEST=y

# The store under test is device-config-store's real state machine over the
# service's slot format; network-manager is here for its types.
CONFIG_NETWORK_MANAGER=y
CONFIG_DEVICE_CONFIG_STORE=y

CONFIG_LOG=y
CONFIG_DEVICE_CONFIG_STORE_LOG_LEVEL=1
