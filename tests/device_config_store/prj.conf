CONFIG_ZTEST=y
CONFIG_DEVICE_CONFIG_STORE=y

# The store reports recovery outcomes through its return values, which is what
# the tests assert on; the log lines are checked by eye when a case fails.
CONFIG_LOG=y
CONFIG_DEVICE_CONFIG_STORE_LOG_LEVEL=2
