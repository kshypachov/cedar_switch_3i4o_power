Сессия Apple Home 2026-09-15 09:48, **плата B (J-Link)**, образ `pka-c-on`: перенос в SRAM, p256-m, PSA-драйвер PKA и крючок ECP P-256. Разбор — `../../../crypto/pka/README.md`. Образ залит заранее через bench MCUboot. Скрипт запускался так: `APPLE_CONSOLE_SERIAL=5AE6020889 apple_home_session.py --no-flash 60`. Перед сессией хранилище стёрто и плата перезагружена. Окно открыто в 09:49:04, владелец остановил сессию в 09:51:20.

**Сопряжение прошло: обе fabric Apple записаны.**

Время по uptime консоли. Для сравнения — плата A, `../20260915-0154`, образ `sram-s3` без PKA и без p256-m.

| Шаг fabric 1 | Плата B, PKA | Плата A, s3 |
|---|---|---|
| PASE: начало → StatusReport | 0,71 с | 1,5 с |
| PASE → ArmFailSafe(60 s): чтения Apple | 3,8 с | 2,7 с |
| ArmFailSafe → AttestationRequest successful | 4,1 с | 3,2 с |
| → CSRRequest successful (второй ArmFailSafe от Apple) | 3,1 с | 5,6 с |
| → AddTrustedRoot successful | 0,56 с | 0,9 с |
| → NOC chain validation successful | 0,59 с | 0,8 с |
| Sigma1 → CASE session established | 1,05 с | 1,5 с |
| CommissioningComplete → «Commissioning completed successfully» | 0,61 с | 0,5 с |
| **Итого** | **16,6 с** | **18,5 с** |

- **Вычисления на плате — десятки миллисекунд:**
  - подпись для Attestation — 20 мс, CSR — 58 мс, проверка NOC-цепочки — 23 мс;
  - CASE: Sigma1→Sigma2 — 0,37 с, Sigma3→сессия — 0,36 с.
- **Что остаётся:** 16,6 с почти целиком — паузы Apple между командами, от 3 до 4 с на шаг, и ожидание обмена. На стороне платы ускорять больше нечего.
- **Fabric 2** (Apple добавляет вторую fabric с fail-safe 30 с): ArmFailSafe(30 s) в 00:01:36,0 → CSR 00:01:36,97 → AddNOC 00:01:39,54 → CASE 00:01:43,21 → fabric 0x2 записана 00:01:44,63. Всего **8,6 с**, с запасом до 30 с (на плате A было ~8 с). Третья CASE-сессия, к узлу 72197CCC fabric 1, установилась в 00:01:49,67.

**Обмен после сопряжения.**
- Apple подписывается (Subscribe) и читает атрибуты. От узла 33CFE2F6 пришло 213 сообщений, от 72197CCC — 74.
- Много повторных отправок платы. 13 сообщений не доставлены после 4 повторов: 9 к 33CFE2F6, 4 к 72197CCC.
- Apple несколько раз повторял один и тот же Subscribe (M:187350698, 5 копий): до него не доходили ответы или подтверждения платы.
- **Сравнение с платой A:** в сессии `20260915-0154` за 8,5 мин было 394 строки повторов и 5 недоставленных сообщений, к тем же двум узлам.
- **Вывод:** потери есть на обеих платах, но на плате B их заметно больше за меньшее время. Это та же картина, что в `../w5500-driver-analysis.md` для платы B: там upstream-драйвер W5500 (правки откачены 2026-09-15). К скорости PKA это не относится.

Прочее в логе — известное:
- `Long dispatch time` 258–513 мс;
- `GetClock_RealTimeMS` не поддерживается;
- ошибки чтения атрибутов на пустом хранилище;
- при старте ошибки FRAM mb85rsxx и MAC.
