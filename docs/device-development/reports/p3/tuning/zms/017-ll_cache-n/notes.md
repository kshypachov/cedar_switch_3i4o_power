В пределах шума, как и следует из settings_zms.c: кеш связного списка используется только при полной загрузке, а Matter читает ключи по хешу (load_one/get_val_len). Без него скан всех записей 707 мс (база ~420), и удаление fabrics — полная загрузка поддерева — в циклах 4–5 выросло до 3,0 / 4,1 с (база 1,6–2,2); медиана это скрывает. Оставить LL-кеш включённым.
