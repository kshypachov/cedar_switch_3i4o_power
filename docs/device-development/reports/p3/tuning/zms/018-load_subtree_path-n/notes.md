В пределах шума: LOAD_SUBTREE_PATH влияет только на загрузку поддерева по имени (settings_zms_load_subtree), а Matter читает ключи через load_one/get_val_len, удаление fabrics обходит поддерево целиком. Рост старта 0,78 с/цикл против 0,57–0,66 у баз — внутри разброса. Оставить включённой, как в базе.
