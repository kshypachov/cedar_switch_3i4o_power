# Следующие прогоны после первого прохода (собирать, когда очередь закончится)

ZMS (опора 4,44 с, шум 0,23 с):
- уточнение кеша: CONFIG_ZMS_LOOKUP_CACHE_SIZE=1536 (плато 2048/4096 = 2,67; 1024 = 3,21; 512 хуже базы)
- кеш × хеш: 2048 + CONFIG_ZMS_LOOKUP_CACHE_FOR_SETTINGS=n; 1024 + FOR_SETTINGS=n (015: при 256 FOR_SETTINGS=n лучше, 3,89)
- секторы: 4 лучше по медиане, но GC-всплески 8,1 / 5,3 с — проверить 2–3 сектора только если в комбинации с кешем 2048 есть смысл; 8/16/64 при 5 циклах неинформативны (до GC не доходят)
- NO_LL_DELETE: удаление растёт 1,5 → 5,8 с — в комбинацию только после 10 циклов
- SPI_STM32_INTERRUPT=y лучше базы (026: 3,55, −0,82), хотя bench медленнее — проверить: кеш 2048 + interrupt (и отдельно кеш 2048 без него как опора комбинации)
- секторы/NO_LL_DELETE/collision bits/block size/LL cache/LOAD_SUBTREE_PATH/40 МГц/fast read — в комбинацию не брать (шум или вред на длинной дистанции)
- комбинация лучших → 5 циклов → 10 циклов (--cycles 10, kind confirm)
- 00:10 — 030-comboA (кеш 2048 + interrupt) = 2,41 с, лучший. Итог выбирать после 031–035; затем confirm: тот же образ, 5 циклов (kind confirm) и 10 циклов (--cycles 10). Образ: builds/zms-comboA (или победитель 032–035). Сборка не нужна — вставить в очередь перед File нельзя (File уже идёт) → поставить после File или прервать File между попытками по решению.

ZMS после подтверждения (02:15): 037 comboA 5 циклов 2,50 (повтор 030); 038 comboA 10 циклов 2,72, рост 0,03 с/цикл;
039 comboA + 4 сектора 10 циклов 2,30 — без роста, GC 0,8 с раз в ~5 циклов.
- ОТКРЫТО: запас места при 4 секторах (16 КБ, ~12 КБ под данные) для 5 fabrics — замерить объём живых
  данных ZMS после commissioning 5 fabrics (alloc/data wra до и после, `settings list` с размерами);
  если не хватает — comboA + 8 секторов, 10 циклов. Спросить владельца, прежде чем закрывать итог ZMS.

File: заполнить после первого прохода.

Общее: DMA для SPI1 пропущен — CONFIG_DCACHE=y, драйвер требует nocache-буферы (вопрос владельцу).

Общий открытый пункт (02:31): проверка на 5 fabrics — методика владельца всегда вводит один fabric.
Касается двух кандидатов: ZMS 4 сектора (16 КБ) и File MAX_LINES < ~90 (004: 64 = 31,00 с против 53,33;
при 5 fabrics ~58 живых записей → сжатие почти на каждой записи). Предложить владельцу отдельный
прогон: commission 5 разных fabrics подряд, замер commit/старта/удаления и объёма данных, для лучших
кандидатов обоих бэкендов. Без решения владельца в итоги не включать.

File после первого прохода (08:24):
- помогают: MAX_LINES (главное; значение — только после проверки на 5 fabrics), read-size 32 (−2,8), SPI interrupt (−9,7, по всем метрикам);
- хуже/срыв: MAX_LINES 256, read-size 1024, cache-size 256/512, fast read off; 4096 поднимает пики;
- не влияют: prog-size, lookahead, block-cycles, раздел 1 МБ, FC_HEAP_SIZE, NUM_FILES, SPI 40 МГц.
- СЛЕДУЮЩЕЕ после очереди (плата свободна): собрать file-comboA = read-size 32 + SPI_STM32_INTERRUPT (MAX_LINES базовый 128),
  прогнать 5 циклов (kind combo), затем повтор 5 и 10 циклов (kind confirm). Вариант с MAX_LINES — по решению владельца о 5 fabrics.
