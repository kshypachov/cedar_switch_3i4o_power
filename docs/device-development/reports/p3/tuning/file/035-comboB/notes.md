comboB = SPI_STM32_INTERRUPT=y + SPI_NOR_SLEEP_WHILE_WAITING_UNTIL_READY=n: хуже базы, 60,61 с (+7,31), хотя по отдельности обе ускоряли (027 interrupt 43,59; 028 без сна 51,70). Старт с fabric 8,5 / 23,5 / 54,8 / 7,6 / 23,9 с (027: 6,7 / 16,8 / 32,3 / 6,5 / 16,8; база 7,8 / 20,7 / 48,1 / 7,1 / 21,2), удаление 15,1 / 33,6 / 67,8 / 15,3 / 34,4 с (027 до 41,0; база до 60,8), commit 1,9–3,9 с (027 1,4–2,6). Commissioning 5/5, сохранность 13/13, лишних загрузок нет. bench: чтение ключа 525 мс (027 368, база 310), open+close 19,9 мс, read 32 33,3 мс.

Проверено, что сборка ни при чём: `.config` отличается от образа 027 только двумя строками SPI_NOR_SLEEP_*, zephyr.dts тот же; исходники драйверов SPI/флеша, littlefs, settings, kvss, приложения после сборки 027 (2026-09-13 19:12) не менялись. У 032 (read-size 32 + interrupt) `.config` совпадает с 027 полностью.

Второе подряд сочетание с interrupt хуже базы, а bench медленнее и там, где ожидания готовности флеша нет (чтение ключа). Объяснение не выбрано до 036 — повтора interrupt на том же образе, что 027 (sha256 a8e0c3f99458): ~43,6 с — сочетания правда мешают друг другу (для comboB гипотеза: опрос WIP без сна в режиме прерываний — поток спит на каждой короткой транзакции опроса); ~60 с — изменилась плата или окружение, и 027 надо перепроверить.
