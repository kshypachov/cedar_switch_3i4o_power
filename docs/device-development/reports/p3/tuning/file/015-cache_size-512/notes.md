cache-size 512 (при read-size 256): сильно хуже базы, commissioning 4/5 из-за хранилища (узел 104 — PASE timeout, поток Matter занят). Старт с fabric 18,3 / 45,4 / 78,0 / 17,0 / 19,8 с (база 7,8 / 20,8 / 48,0 / 7,0 / 21,1), без fabric до 66,0 с, удаление 25,3 / 59,0 / 100,6 / 1,1 (без fabric) / 27,5 с (база до 60,8). Оценка 49,63 не показательна: удаление цикла 4 без fabric (1,07 с). Повтор не нужен.

Противоречие: storage_bench быстрее базы (чтение ключа 266 мс против 310, read 32 +seek 9,8 мс против 17,4), а путь Matter медленнее уже в цикле 1. Бенч идёт на простаивающей системе и только читает; у Matter чтения чередуются с дописыванием в тот же файл. Гипотеза, не проверенная по коду: общий кеш чтения/записи 512 байт сбрасывается на каждой записи. На выбор не влияет — база 2048 лучше.

`fs: file read error (-14)` в логе — не дефект кеша: на первой загрузке после прошивки, через секунду после `storage_wipe yes` и до перезагрузки (раздел стёрт под смонтированной LittleFS, LFS_ERR_CORRUPT → -EFAULT). Артефакт методики; на замеренных загрузках ошибок LittleFS нет ни в одной попытке.
