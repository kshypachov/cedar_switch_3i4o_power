MAX_LINES 48: 23,54 с — лучший File на один fabric по всем метрикам, не только по медиане: старт с fabric среднее/макс 9,9/14,2 с (база 20,9/48,0; 64: 12,4/17,7), удаление 12,3/13,7 с (база 29,7/60,8), commit 1,7–2,6 с, чтение ключа 94 мс, noc_to_done 31,2 с. Пила почти исчезла: сжатие очень частое.

Непригодно для устройства: живых записей ~26 при одном fabric, 37 при двух, ~58 при пяти (STATUS). Когда живых записей больше MAX_LINES, сжатие не опускает файл ниже порога и идёт на каждой записи — полная перезапись файла на каждое сохранение; при 5 fabrics 48 < 58. Меньший MAX_LINES улучшает метрику на одном fabric, а предел задаёт число живых записей при максимуме fabrics — MAX_LINES для File нужно подбирать на целевом числе fabrics, не на одном.
