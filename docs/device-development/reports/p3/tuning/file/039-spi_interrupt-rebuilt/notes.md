Контроль: SPI_STM32_INTERRUPT=y, пересобранный 2026-09-14 11:03 на текущих исходниках (eth_w5500.c с MMB и rx resync; esp_hosted_mcu изменён в 10:07). **59,65 с (+6,35) — хуже базы**, тогда как тот же `.config` на образе 2026-09-13 19:12 даёт 43,59 / 43,49 / 43,62 (027, 036, 037). Старт с fabric 9,0 / 23,1 / 52,0 / 8,1 / 23,7 с, удаление 15,3 / 33,1 / 64,8 / 15,3 / 33,8 с, commit 3,5 с, старт на чистом разделе 2,30 с (027: 1,36). Замедлены и фазы без хранилища: attestation 4,9 с (027: 3,3), csr 3,9 (3,7). bench: чтение ключа 514 мс (368), open+close 17,7 мс (8,9). Commissioning 5/5, сохранность 13/13, лишних загрузок нет.

Пересобранная база без interrupt (038) при этом не замедлилась (52,84). Значит, на текущих исходниках interrupt File больше не ускоряет, а замедляет (59,65 против 52,84). Выводы 032/033/035 («сочетания мешают interrupt») сняты: на новых исходниках comboA 59,18 / 58,88 и comboB 60,61 — на уровне самого interrupt (59,65), то есть read-size 32 и NOR без сна к нему почти ничего не добавляют и не отнимают.

Проверено: `.config` 027 и 039 одинаковый; по `nm` различаются только eth_w5500 (w5500_rx, w5500_rx_resync) и esp_hosted_mcu; у comboA/comboB (собраны до 10:07) — только eth_w5500. W5500 на spi2, флеш настроек на spi1 — шина общая только режимом прерываний (CONFIG_SPI_STM32_INTERRUPT глобален). Замедление общесистемное (криптофазы), причина не установлена; диагностика загрузки потоков без сборки — tuning/diag/.
