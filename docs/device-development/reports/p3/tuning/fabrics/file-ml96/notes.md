File, MAX_LINES 96, опрос SPI (15:04–15:44): **не годен — потолок 3 fabrics, раунд 2 хуже раунда 1**. Commissioning 6/10, сохранность 14/14, лишних загрузок 0, ошибок хранилища нет — все отказы по времени.

Раунд 1 до секунд как MAX_LINES 128: fabrics 1–3 прошли (53,8 / 58,5 / 78,4 с), старт с 1 / 2 / 3 fabrics 6,8 / 30,2 / 96,3 с, 4-й и 5-й — «Failsafe timer expired». При 3 fabrics в файле ~44 записи — меньше и 96, и 128, поэтому до этой точки MAX_LINES ни на что не влияет. Удаление 3 fabrics 201,8 с (128: 340,6) — сжатие файла срабатывает раньше.

Раунд 2 хуже: старт уже с 1 fabric 54,2 с (раунд 1: 6,8), 2-й commissioning сорван — поток Matter занят 17 с («Long dispatch time: 16958 ms»), CASE-сессия истекла; 3-й и 4-й прошли (82,9 и 75,8 с), 5-й сорван через 12,7 с — PASE timeout, на плате «Long dispatch time: 27750 ms». Адрес chip-tool разрешал нормально («Resolve failure … cancelled» — отмена второго поиска по другому интерфейсу), отказы — занятость потока Matter хранилищем, не сеть. Старт с 3 fabrics 126–129 с, удаление 290,5 с. bench: раунд 1 — 4873 Б / 44 записи, чтение ключа 863 мс; раунд 2 — 4285 Б / 41 запись, 989 мс.

Вывод: меньший MAX_LINES не поднимает потолок — 3 fabrics как у 128, а сжатия на пути Matter делают отдельные операции длиннее (отказы уже на 2-м fabric во 2-м раунде).
