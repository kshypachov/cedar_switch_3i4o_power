Прошивка ZMS 4 сектора + кеш поиска 2048 (prj.conf после переноса) + `CONFIG_SPI_STM32_INTERRUPT=y`, с `common.conf`, 16:47–17:01, по запросу владельца: **годна и быстрее варианта без прерываний** — 10/10 commissioning, 5 fabrics в обоих раундах, сохранность 14/14, лишних загрузок 0, ошибок хранилища 0. Оценка 2,63 + 8,69 = 11,3 с (тот же конфиг без прерываний `zms-prod-sectors4`: 12,3).

Сборка отличается от `prod-zms-shell` ровно строкой `CONFIG_SPI_STM32_INTERRUPT=y` в `.config`; подпись кода eth_w5500/esp_hosted одинакова (634ffb5b67d2) — исходники между двумя сборками не менялись.

Старт с k = 1…5 fabrics: раунд 1 — 1,32 / 1,39 / 1,79 / 2,24 / 2,63 с, раунд 2 — 1,13 / 1,49 / 1,82 / 2,16 / 2,53 с (без прерываний: 1,25…2,81 и 1,29…2,74 — на 5 fabrics −0,19 и −0,21 с). Удаление 5 fabrics 6,7 / 8,7 с (9,3 / 9,5). Максимальный commit 1,36 с (2,45 с — всплеск сборки мусора короче). Чтение ключа 0,9 мс (1,2). Commissioning 52,8–56,9 с — как без прерываний (53,1–57,8): его время — криптография и сеть, не хранилище.

Оговорка: у File выигрыш прерываний зависел от сборки (43,5 / 59,6 с при одном `.config`, см. diag/README.md). Здесь сравнение честное — два образа из одних исходников, — но это одна пара сборок. Перед включением прерываний в prj.conf повторить пару на другой сборке. Решение владельца.
