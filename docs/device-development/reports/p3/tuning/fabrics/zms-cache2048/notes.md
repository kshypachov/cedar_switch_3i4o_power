ZMS, кеш поиска 2048, 32 сектора, опрос SPI (13:39–13:53): **годен** — 10/10 commissioning, 5 fabrics в обоих раундах, сохранность 14/14, лишних загрузок 0, ошибок хранилища 0. Оценка (макс. старт с 5 fabrics + макс. удаление 5) 3,76 + 11,37 = 15,1 с.

Старт Matter с k = 1…5 fabrics: раунд 1 — 1,13 / 1,48 / 1,94 / 2,36 / 2,76 с, раунд 2 — 1,44 / 1,95 / 2,64 / 3,29 / 3,76 с; без fabrics после удаления 1,18 и 1,09. Рост ~0,4 с на fabric в раунде 1 и ~0,6 в раунде 2. Commissioning 53,1–56,9 с (раунд 1) и 54,7–60,7 (раунд 2), commit 0,07–0,41 с и 0,32–1,30 с, noc_to_done 29,2–31,7 с. Удаление 5 fabrics 8,6 и 11,4 с. bench при 5 fabrics: 61 запись, чтение ключа 1,2 мс, скан всех 132 и 219 мс.

Раунд 2 медленнее раунда 1 при том же числе fabrics — ZMS копит историю (старые ATE, узлы связного списка удалённых ключей), а сборки мусора ещё не было: позиция записи дошла только до сектора 7 из 32 (`alloc wra: 7, 2c0`), ~0,45 сектора на fabric и ~1,5 сектора на удаление. За весь прогон записано ~7,1 сектора ≈ 29 КБ с историей. Отсюда прогноз для меньших разделов: 8 секторов (32 КБ) — сборка мусора во 2-м раунде; 4 сектора (16 КБ) — уже в 1-м раунде около 3-го fabric.

Живых записей при 5 fabrics 61 (оценка по дампам P3 — ~67). Все `<err>` в консоли не от хранилища: `Long dispatch time` (Matter, до 10 с на PASE), `mb85rsxx: invalid device ID` (FRAM на плате не отвечает, на каждой загрузке), `net_ethernet: Invalid MAC address`, `Fail to retrieve data … 0x0030/0x000C`, отсутствие RTC.
