File, MAX_LINES 128 (как в прошивке), опрос SPI (14:22–15:04): **не годен — больше 3 fabrics не держит**. В обоих раундах commissioning 1–3 прошли, 4 и 5 сорвались по хранилищу: «Failsafe timer expired», плата откатывает незавершённый fabric («Reverting pending fabric data»). Итого 6/10, макс. 3 fabrics; сохранность 14/14, лишних загрузок 0, `<err>` от хранилища нет — отказ по времени, не по ошибке.

Старт Matter с k fabrics: 1 — 6,8 / 8,4 с, 2 — 30,4 / 31,6 с, 3 — 96,3 / 92,6 с (раунд 1 / 2), после неудачных 4-го и 5-го с теми же 3 fabrics 99–105 с. Рост примерно втрое на fabric: каждое чтение настройки у File загружает весь файл (settings_file.c без load_one), а Matter при старте читает ключи каждого fabric по одному. Commissioning: 53,5 / 58,4 / 78,1 с для 1–3-го; noc_to_done 29,8 → 32,9 → 46,9 с; commit 1,9–3,3 с. На 4-м fabric поток Matter занят «Long dispatch time» 21–22 с подряд, окно открывается 12–14 с, и 60 с fail-safe истекает до CommissioningComplete (как 007 на одном fabric). Раунд 2 повторяет раунд 1 до секунд — предел стабильный.

Удаление 3 fabrics 340,6 и 325,7 с (ZMS удаляет 5 за 9–14 с). bench при 3 fabrics: файл 4873 / 4990 Б, 44 записи, чтение ключа 863 / 840 мс, open+close 14,3 / 6,2 мс.

Вывод: у File в режиме опроса SPI при MAX_LINES 128 потолок — 3 fabrics из 5, допускаемых Matter; оценка не считается. MAX_LINES 96 и 64 держат файл короче — проверяются следом.
