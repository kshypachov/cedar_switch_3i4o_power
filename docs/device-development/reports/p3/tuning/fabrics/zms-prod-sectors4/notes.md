Прошивка после переноса в репозиторий (prj.conf: ZMS 4 сектора, кеш поиска 2048, опрос SPI; раздел storage_zms), с `common.conf` (шелл settings для скрипта), 16:30–16:45: **годна, повторяет измеренного кандидата** — 10/10 commissioning, 5 fabrics в обоих раундах, сохранность 14/14, лишних загрузок 0, ошибок хранилища 0. Оценка 2,81 + 9,47 = 12,3 с (кандидат `zms-cache2048-sectors4`: 13,1).

`.config` этого образа идентичен измеренному `fab2-zms-cache2048-sectors4`, `zephyr.dts` отличается только именем раздела. Старт с k = 1…5 fabrics: раунд 1 — 1,25 / 1,51 / 1,94 / 2,41 / 2,81 с, раунд 2 — 1,29 / 1,64 / 2,01 / 2,32 / 2,74 с (кандидат: 1,13…2,76 и 1,20…2,68 — в пределах 0,1 с). Удаление 5 fabrics 9,3 / 9,5 с (кандидат 10,3 / 9,5). commit до 0,97 с и тот же всплеск 2,45 с при сборке мусора (раунд 2, 3-й fabric). Чтение ключа 1,2 мс.

Перед этим — загрузка образа «как поставляется» без шелла (`../prod-boot/boot.json`): после прошивки и после перезагрузки из шелла «fs_zms: 4 Sectors of 4096 bytes», Matter готов на 3,9 и 4,1 с аптайма, ошибок хранилища нет, позиция записи ZMS сдвигается (данные пишутся и читаются после перезагрузки).
